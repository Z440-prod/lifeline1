// ─────────────────────────────────────────────────────────────────────────────
// Your Firebase project config. PASTE YOUR OWN VALUES HERE.
//
// Get them from: Firebase Console → Project settings (⚙️) → "Your apps" →
// register a Web app (</>) → copy the firebaseConfig object.
//
// NOTE: these values are NOT secret. The Web API key just identifies your
// project; it is meant to live in client code. Your data is protected by the
// Firestore security rules (firestore.rules), not by hiding this key.
// ─────────────────────────────────────────────────────────────────────────────
export const firebaseConfig = {
    apiKey: "REPLACE_ME",
    authDomain: "REPLACE_ME.firebaseapp.com",
    projectId: "REPLACE_ME",
    storageBucket: "REPLACE_ME.appspot.com",
    messagingSenderId: "REPLACE_ME",
    appId: "REPLACE_ME",
};
